[project]
name = "mnms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.11.0",
    "torchcodec>=0.11.1",
]

[tool.uv.workspace]
members = [
    "transcription",
    "meta",
    "ocr",
]
