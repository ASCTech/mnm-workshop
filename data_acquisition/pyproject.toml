[project]
name = "data_acquisition"
version = "0.1.0"
description = "Polite, capped retrieval of openly-licensed corpora for the workshop branches"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.0",
    "python-dotenv>=1.2.2",
    "beautifulsoup4>=4.12",
]
